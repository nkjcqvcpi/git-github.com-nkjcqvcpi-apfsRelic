{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. Application-defined commands (ls, recover, config, ...) are allowed by default and the native dialogs are driven from Rust, so no plugin permissions are needed here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
